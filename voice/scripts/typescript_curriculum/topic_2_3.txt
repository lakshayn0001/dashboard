Audio briefing for 2.3 Narrowing and type guards. Core concept: Type predicates ('acc is Admin') and assertion functions ('asserts acc is Admin') teach the compiler control flow narrowing for the scope. Key principles: Control flow analysis is how TS becomes usable. x is User predicates teach the checker. Why this matters in production: This is daily-driver TypeScript. Without it you will fight the compiler. Common pitfall to avoid: Type predicates that lie (checker trusts the signature unconditionally). Using typeof on custom classes expecting a string tag. Practical takeaway: User-defined type guards use the syntax 'param is Type' in their return type position to refine types across branches.