# Standalone Voice Engine Dependencies
# Python 3.11 / 3.12 / 3.13

# Core numerical and audio I/O
numpy>=1.26.0
scipy>=1.12.0
soundfile>=0.12.1

# Default Provider: Kokoro TTS (82M open weights, 24kHz)
kokoro>=0.8.4

# Zero-key fallback provider: Microsoft Edge Neural TTS
edge-tts>=6.1.12

# Optional commercial cloud providers (active only when keys exist in env)
# azure-cognitiveservices-speech>=1.38.0
# elevenlabs>=1.50.0
