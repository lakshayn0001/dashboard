# Voice engine. The published dashboard speaks with Edge neural TTS only.
# Python 3.11 / 3.12 / 3.13

numpy>=1.26.0
scipy>=1.12.0
edge-tts>=6.1.12

# Supplies an ffmpeg binary on machines that do not have one (macOS without Homebrew).
# GitHub Actions installs ffmpeg with apt and does not need this.
imageio-ffmpeg>=0.5.1

# Optional. Not used for published audio.
# kokoro>=0.8.4
# soundfile>=0.12.1
